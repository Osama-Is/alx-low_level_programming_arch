#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @fnum: first operand number
 * @snum: second operand number
 *
 * Return: Sum
 */
int add(int fnum, int snum)
{
	int sum = fnum + snum;

	return (sum);
}
